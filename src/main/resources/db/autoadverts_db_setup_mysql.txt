1) Download and install the MySQL database,
   which can be found here: http://dev.mysql.com/downloads/.

2) Create user and schema. (Default datasource values: schema = "auto_ads"; user = "autoads"; password = "7x9zXD5i")

3) Import:
    - structure.sql;
    - roles.sql;
    - set_admin_here.sql;
    - engine_types.sql (optional);
    - autocatalog.sql (optional).

4) Configure datasource.